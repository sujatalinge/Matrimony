@model IEnumerable<CRUDUsingEFDBFirstApproach.Models.Trainer>

<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<script src="~/Scripts/jquery-1.9.1.min.js"></script>
<script src="~/Scripts/bootstrap.min.js"></script>
<p>
    @Html.ActionLink("Create New", "Create")

    <div class="container"></div>
</p>
<table class="table table-bordered table-striped table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary)
            </td>
            <td>
                @Html.ActionLink(".", "Edit", new { id = item.Id },new { @class = "glyphicon glyphicon-pencil" }) |
                @Html.ActionLink(".", "Details", new { id = item.Id },new { @class= "glyphicon glyphicon-file"}) |
                @Html.ActionLink(".", "Delete", new { id = item.Id},new { @class = "glyphicon glyphicon-trash bg-danger" }) |
            </td>
        </tr>
    }

</table>
  </div>


